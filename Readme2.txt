To Receive IR values from the remote controller:

* Arduino ide --> File --> Examples --> IRremote --> IRrecvDemo

* Upload the code and open the serial monitor under tools to view the values received.