Arduino IR library:

* Extract the contents of the Arduino-IRremote-master.zip to a folder.

* Copy the folder and paste is in the location --> C:\Program Files (x86)\Arduino\libraries

* !!! Delete the 'RobotIRremote' library from the libraries folder. !!!
